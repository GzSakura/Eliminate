plugins {
    id 'fabric-loom' version '1.14-SNAPSHOT' apply false
    id 'net.neoforged.gradle.userdev' version '7.0.117' apply false
}

allprojects {
    version = project.mod_version
    group = project.maven_group
}
